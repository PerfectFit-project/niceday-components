#!/bin/bash
if [[ ! -d "./build/goalie-js" ]]; then
    git clone git@github.com/PerfectFit-project/goalie-js.git --single-branch --branch perfectfit-patch ./build/goalie-js || exit 2
fi

cd ./build/goalie-js
yarn install
tsc --sourceMap -p .
